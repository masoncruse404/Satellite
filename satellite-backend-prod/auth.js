
import jwt from "jsonwebtoken";

const auth = async (request, response, next) => {
  try {
    const token = request.headers 
  } catch (error) {
    console.log(error)
    
  }
};

export default auth;
